#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>

int putsmystring(char *);
int convert_this(int);
unsigned int binane(unsigned int);
int putsmychar(char);
int _printf(const char *, ...);

#endif
